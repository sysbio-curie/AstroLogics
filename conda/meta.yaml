{% set name = "astrologics" %}
{% set version = "1.0.0" %}
package:
  name: '{{ name|lower }}'
  version: '{{ version }}'

source:
  path: ..

build:
  noarch: python

requirements:
  build:
    - python >3.8
    - setuptools

  run:
    - python >3.8
    - pandas
    - h5py
    - scipy
    - scikit-learn
    - tslearn
    - fastcluster
    - seaborn
    - jupyterlab
    - ipylab
    - pymaboss
    - boolsim-python 
    - mpbn
    - tqdm
    - pip

test:
  imports:
    - astrologics
  
  
about:
  home: https://github.com/sysbio-curie/AstroLogics
  summary: 'A simulation-based framework for the analysis of Boolean model ensembles.'

